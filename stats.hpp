///////////////////////////////////////////////////////////////////////////////
// stats.hpp
//
// Example of skeleton code that can be delivered to students for an
// assignment involving basic statistics on vectors of numbers. This
// code would be given to students; the unit tests can compile against
// it, but all the tests fail since the function bodies are stubs.
//
///////////////////////////////////////////////////////////////////////////////

#pragma once

#include <cassert>
#include <vector>

// Given a non-empty vector of numbers, return the maximum element.
int stat_max(const std::vector<int> numbers) {
  assert( !numbers.empty() );
  // TODO: rewrite this function to actually do what it's supposed to
  return 0;
}

// Given a non-empty vector of numbers, return the mean average.
int stat_mean(const std::vector<int> numbers) {
  assert( !numbers.empty() );
  // TODO: rewrite this function to actually do what it's supposed to
  return 0;
}

// Given a non-empty vector of numbers, return the median.
int stat_median(const std::vector<int> numbers) {
  assert( !numbers.empty() );
  // TODO: rewrite this function to actually do what it's supposed to
  return 0;
}
